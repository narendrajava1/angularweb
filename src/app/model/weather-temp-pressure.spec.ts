import { WeatherTempPressure } from './weather-temp-pressure';

describe('WeatherTempPressure', () => {
  it('should create an instance', () => {
    expect(new WeatherTempPressure()).toBeTruthy();
  });
});
